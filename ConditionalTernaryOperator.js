// the conditional Tenary operator is a shorthand of the if elase statement

//SYNTAX

// Condition ? ExpressionIfTrue : ExpressionIfFalse



// const k = prompt("input");
// const action = k >= 18 ? "Drink Bear" : "Drink Juice";
// console.log(action)

// if (k >= 18) {
//     console.log(`Drink Bear because you are ${k}`);
// }
// else
//     console.log("Drink Juice");

// // Note that conditional Tenary operator works fine in template literals see the example below. How ever the if statement and the switch statements do not work with template literals
// console.log(`i need to ${k >= 18 ? "Drink Bear" : "Drink Juice"} this afternoon`)



